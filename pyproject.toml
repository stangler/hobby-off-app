[project]
name = "hobby-off-generator"
version = "0.1.0"
description = "Hobby OFF image generator"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "pillow>=10.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false